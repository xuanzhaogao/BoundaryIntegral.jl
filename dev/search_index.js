var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Internals:-truncated-kernel-method","page":"API reference","text":"BoundaryIntegral.TKM3D evaluates the free-space Laplace potential of volume sources with a truncated-kernel spectral method. It is used internally by the volume-source right-hand sides and by PrecomputedVolumeField.","title":"Internals: truncated-kernel method"},{"category":"type","location":"api/#BoundaryIntegral.BatchResult","page":"API reference","text":"BatchResult\n\nEverything the post-eval phase needs from one solved batch: the raw truncated pair densities (global-grid indexed), the adapted interface, the layer densities Σ, and solve stats. Positions are NOT stored — they are recomputed from gidx + the template. Files are tied to the exact code version that wrote them — the version field guards the BatchResult layout only, not the serialized interface type; after changing interface/panel struct layouts, re-solve affected batches.\n\n\n\n\n\n","title":"BoundaryIntegral.BatchResult"},{"category":"type","location":"api/#BoundaryIntegral.BatchedDielectricOperator","page":"API reference","text":"BatchedDielectricOperator\n\nMatrix-capable adjoint-double-layer + diagonal operator for the dielectric BIE. A single batched FMM (nd = K) serves an N x K block; the sparse near correction and the diagonal contrast term are applied as batched mat-mat / broadcasts.\n\n\n\n\n\n","title":"BoundaryIntegral.BatchedDielectricOperator"},{"category":"type","location":"api/#BoundaryIntegral.CampaignInput","page":"API reference","text":"CampaignInput\n\nParsed .toml: the full system (orbitals, dielectrics, solve params) plus campaign batching/eval settings. Template grids are NOT loaded here (see load_templates!). Orbital id = 1-based index in orbitals.\n\n\n\n\n\n","title":"BoundaryIntegral.CampaignInput"},{"category":"type","location":"api/#BoundaryIntegral.CenterInfo","page":"API reference","text":"CenterInfo(id, template_id, steps, center)\n\nOne orbital site: 1-based id, template_id (index into CampaignInput.templates), the integer global-grid offset steps, and the realized Cartesian center.\n\n\n\n\n\n","title":"BoundaryIntegral.CenterInfo"},{"category":"type","location":"api/#BoundaryIntegral.LatticeBatch","page":"API reference","text":"LatticeBatch\n\nA batch of pair densities on the union of their supports, indexed on the VIRTUAL GLOBAL GRID (gidx = integer grid coordinates in the template frame). All columns share gidx/positions/weights — required for the nd-batched FMM.\n\n\n\n\n\n","title":"BoundaryIntegral.LatticeBatch"},{"category":"type","location":"api/#BoundaryIntegral.NearFieldGeometry","page":"API reference","text":"NearFieldGeometry{T}\n\nSection 3 near/far geometry for one volume source. lo/hi are the corners of the padded near region B_pad (Eq. 3.9), center is the centre of B (which is also the centre of B_pad), l the side lengths of B (Eq. 3.7), h the characteristic spacing (Eq. 3.3), hn = c_pad * h (Eq. 3.10), L the truncation radius (Eq. 3.11), and dks the Fourier spacings (Eq. 3.16 at equality).\n\n\n\n\n\n","title":"BoundaryIntegral.NearFieldGeometry"},{"category":"type","location":"api/#BoundaryIntegral.OrbitalInstance","page":"API reference","text":"OrbitalInstance(id, template_id, steps)\n\nOne orbital of a lattice campaign: template template_id translated by the integer global-frame offset steps (grid steps; see lattice_grid_steps).\n\n\n\n\n\n","title":"BoundaryIntegral.OrbitalInstance"},{"category":"type","location":"api/#BoundaryIntegral.OrbitalSpec","page":"API reference","text":"OrbitalSpec(type, pos)\n\nOne orbital from the .toml [[orbital]] list: type indexes CampaignInput.templates (1-based), pos is its Cartesian center in the templates' frame.\n\n\n\n\n\n","title":"BoundaryIntegral.OrbitalSpec"},{"category":"type","location":"api/#BoundaryIntegral.PhaseRunner","page":"API reference","text":"PhaseRunner(toml_path, phase)\n\nCallable mapped over pending batch ids by run_phase. Lives in the core package — not the Distributed extension — because pmap serializes its function to workers, and workers load only BoundaryIntegral (never the extension, which needs SlurmClusterManager). An extension-owned closure cannot be deserialized there: the worker hits KeyError: PkgId(... BoundaryIntegralDistributedExt) not found, the failure wedges the message stream, and pmap waits forever.\n\n\n\n\n\n","title":"BoundaryIntegral.PhaseRunner"},{"category":"type","location":"api/#BoundaryIntegral.PrecomputedVolumeField","page":"API reference","text":"PrecomputedVolumeField(vs; tol, kmax = nothing, c_pad = 5.0,\n                       compute_pot = true, compute_grad = true,\n                       cache_fft = false, cache_fft_pad = 1.25,\n                       nthreads = min(TKM3D._ltkm_default_nthreads(), Sys.CPU_THREADS))\n\nTarget-independent precomputed spectral representation of the free-space Laplace potential (1/(4π r) convention) of a VolumeSource, for evaluation at many target batches without redoing per-call setup.\n\nConstruction (once): fix the evaluation box Bpad = source box B (Eq. 3.7) padded by `cpad * hwhereh = ||Arho||2(Eq. 3.3); the Fourier box follows Eqs. (3.11) and (3.16) vianearfieldgeometry. On that Fourier box, run the type-1 NUFFT of all source charges, apply the truncated Laplace kernelTKM3D.truncatedlaplace3dhat`, and (optionally) materialize the spectral gradient coefficients.\n\nEvaluation (per batch): targets inside B use a type-2 NUFFT on the stored coefficients — near the source support this spectral evaluation is the only accurate option (point-charge summation has O(1%) quadrature error there); the FFT dimensions never change, so FFTW planning is reused. Targets outside B are well separated from the support and are evaluated with the FMM (lfmm3d at the field tolerance) — equivalent to the exact point sum within tolerance and asymptotically scalable in the batch size.\n\nThis removes the dominant cost of the RHS-adaptive mesh build, where the target-dependent Fourier box previously forced a fresh type-1 NUFFT + FFT plan at every refinement depth (~23 s per call on the production monolayer density; measured 16x build speedup with max rel deviation 1.4e-5 and no refinement-decision changes).\n\nnthreads caps the internal FINUFFT transforms (both the construction-time type-1 and every evaluation's type-2), mirroring TKM3D.ltkm3dc's _ltkm_default_nthreads() default of 16 (override via env TKM3D_FINUFFT_NTHREADS): FINUFFT's default FFTW planner picks a pathological high-thread-count plan for these mode grids otherwise, and without this cap raising Julia's thread count degrades this path rather than speeding it up. The default additionally floors that 16 at Sys.CPU_THREADS so a small Slurm allocation (4-8 CPUs) doesn't oversubscribe — a strict improvement over ltkm3dc's unconditional default, not a divergence from it; Sys.CPU_THREADS reports the machine total and can over-report inside a cgroup-limited allocation, so TKM3D_FINUFFT_NTHREADS remains the precise control for batch jobs. The FMM branch (lfmm3d, out-of-box targets) is unaffected.\n\nMemory: coeff holds prod(nmodes) complex doubles and grad_coeff three times that (≈1 GB and ≈3 GB at the production kmax); construction with compute_grad = true transiently holds both even if compute_pot = false. Use the compute_pot/compute_grad flags to store only what the consumer needs.\n\nCached-FFT mode (cache_fft = true, experimental): the standard in-box path re-runs the FFT of the full coefficient grid inside FINUFFT on EVERY type-2 exec (~0.4 s at production scale, independent of the target count). With cache_fft = true that FFT is done once at construction: the spectral coefficients are divided by the FINUFFT spreading-kernel Fourier factors (pre-deconvolution: the interpolation kernel's smoothing is corrected per mode up front), zero-padded onto a fine grid of cache_fft_pad * nmodes per axis, and inverse-FFT'd; per evaluation a native threaded ES-kernel interpolation reads the stored fine grid (cost ∝ targets, not modes — equivalent to FINUFFT's spreadinterponly type-2 exec, see _field_interponly_type2 for why it is not routed through finufft itself). The coefficient arrays are dropped and replaced by the fine grids (cache_fft_pad^3 ≈ 2x their size at the default 1.25). Accuracy relies on the spectrum having decayed to ~tol at kmax (guaranteed by the kmax estimate); the padding keeps the active modes away from the fine-grid Nyquist, where the upsampfac≈1 interpolation kernel loses accuracy — validated at ~5e-9 max relative deviation from the standard path (pad 1.25; pad 1.0 degrades the gradient to ~2e-6). Transient construction memory peaks at approximately 2× the final cached footprint, as the phi-corrected padded arrays, the bfft workspace, and (if compute_grad = true) all four grids coexist before the coefficient arrays are released (~16 GB at production scale).\n\n\n\n\n\n","title":"BoundaryIntegral.PrecomputedVolumeField"},{"category":"method","location":"api/#BoundaryIntegral._lattice_grid_shift-Tuple{Any, AbstractMatrix, Tuple{Int64, Int64, Int64}}","page":"API reference","text":"_lattice_grid_shift(datagrid, primvec, (n1,n2,n3)) -> NTuple{3,Int}\n\nInteger circshift amounts for translating a density by n1·a1 + n2·a2 + n3·a3, where the datagrid spanning vectors (true cell) are integer multiples of the primitive lattice vectors a_d = primvec[d,:] along each grid axis. Errors if a lattice vector is not commensurate with the grid (i.e. grid-steps-per-lattice-vector is not an integer).\n\n\n\n\n\n","title":"BoundaryIntegral._lattice_grid_shift"},{"category":"method","location":"api/#BoundaryIntegral._primitive_basis-Union{Tuple{T}, Tuple{Tuple{Vector{T}, Vector{T}, Vector{T}}, Tuple{Tuple{T, T, T}, Tuple{T, T, T}, Tuple{T, T, T}}}} where T","page":"API reference","text":"_primitive_basis(axes, basis) -> Union{Nothing, NTuple{3, NTuple{3, T}}}\n\nPrimitive sampling-cell basis A_rho (Eq. 3.2): column j is (axis j step) * basis[j]. axes may hold fractional or absolute coordinates; basis holds the corresponding cell vectors. Returns nothing unless all three axes are uniform with >= 2 points, in which case the samples do not form a lattice.\n\n\n\n\n\n","title":"BoundaryIntegral._primitive_basis"},{"category":"method","location":"api/#BoundaryIntegral._rhs_volume_targets_field-Union{Tuple{T}, Tuple{PrecomputedVolumeField{T}, Matrix{T}, Matrix{T}, T}} where T","page":"API reference","text":"_rhs_volume_targets_field(field, targets, normals, eps_src) -> Vector\n\nDielectric-interface RHS values -(n · ∇φ) / eps_src at targets from a precomputed field (free-space normalization, matching the TKM branch of _rhs_volume_targets_hybrid). Replaces the per-call KDTree classify + lfmm3d/ltkm3dc pair with one field evaluation.\n\n\n\n\n\n","title":"BoundaryIntegral._rhs_volume_targets_field"},{"category":"method","location":"api/#BoundaryIntegral.assemble_lattice_batch-Tuple{AbstractVector, AbstractDict{Int64, OrbitalInstance}, Vector{Tuple{Int64, Int64}}}","page":"API reference","text":"assemble_lattice_batch(templates, instances, pairs; support_rtol=1e-6) -> LatticeBatch\n\nAssemble the pair densities rho_ij = phi_i * phi_j for an explicit pair list. Each orbital is templates[instance.template_id] translated by instance.steps; products are exact pointwise multiplies on the integer frame intersection. The batch lives on the union of the per-pair supports, then truncated by the group envelope (rss across columns ≥ support_rtol × its max — same rule as assemble_rhs_group). All templates must share one grid geometry.\n\n\n\n\n\n","title":"BoundaryIntegral.assemble_lattice_batch"},{"category":"method","location":"api/#BoundaryIntegral.assemble_v-Tuple{CampaignInput}","page":"API reference","text":"assemble_v(c::CampaignInput) -> (; max_rel_asym, n)\n\nFinal phase (single process): gather all V files into the dense n_pairs × n_pairs matrix, write V_full.tsv and report.txt with the max-rel-asymmetry diagnostic.\n\n\n\n\n\n","title":"BoundaryIntegral.assemble_v"},{"category":"method","location":"api/#BoundaryIntegral.batch_volume_sources-Tuple{LatticeBatch}","page":"API reference","text":"batch_volume_sources(b::LatticeBatch) -> Vector{VolumeSource{Float64,3}}\n\nSplit a LatticeBatch into the Vector{VolumeSource} core form (shared positions).\n\n\n\n\n\n","title":"BoundaryIntegral.batch_volume_sources"},{"category":"method","location":"api/#BoundaryIntegral.best_grid-Tuple{Real, Real, Real}","page":"API reference","text":"Return (m, n) for grid tiling of a W×H rectangle into m×n equal rectangles such that each small rectangle's aspect ratio <= alpha and m*n is as small as found by continued-fraction convergents + semiconvergents.\n\nAssumes W>0, H>0, alpha>=1.\n\n\n\n\n\n","title":"BoundaryIntegral.best_grid"},{"category":"method","location":"api/#BoundaryIntegral.best_grid_mn-Tuple{Real, Real, Real}","page":"API reference","text":"Alias for best_grid; kept for backward compatibility.\n\n\n\n\n\n","title":"BoundaryIntegral.best_grid_mn"},{"category":"method","location":"api/#BoundaryIntegral.build_batches-Tuple{Vector{Tuple{Int64, Int64}}, Int64}","page":"API reference","text":"build_batches(pairs, n_centers_per_batch) -> Vector{BatchSpec}\n\nEach pair belongs to its anchor (= min id); consecutive anchors merged n at a time.\n\nThis groups by a fixed ANCHOR COUNT, so the batch size K (= pairs per batch) is whatever the neighbour cutoff happens to give that anchor: on the 198-orbital / 2623-pair benchmark, K runs from 1 to 17 with a mean of 13.2. Prefer the centers/k_target method below, which groups to a fixed K instead – every batch pays a full interface build, corrected LHS operator and pottrg map, so the batch COUNT is what those fixed costs scale with.\n\n\n\n\n\n","title":"BoundaryIntegral.build_batches"},{"category":"method","location":"api/#BoundaryIntegral.build_batches-Tuple{Vector{Tuple{Int64, Int64}}, Vector{CenterInfo}, Int64}","page":"API reference","text":"build_batches(pairs, centers, k_target) -> Vector{BatchSpec}\n\nPartition the pairs into batches of approximately k_target pairs each, keeping the pairs of a batch spatially together.\n\nWhy K and not an anchor count: a batch's cost is (fixed work) + K x (per-pair work). The fixed part – one adaptive interface, one corrected LHS operator, one pottrg map over the evaluation targets – is paid once per batch regardless of K, so the batch COUNT drives it. Grouping by anchor count leaves K to the cutoff geometry (1 to 17 on the 198-orbital benchmark, mean 13.2), giving both more batches than necessary and a wide spread of per-batch cost, which unbalances a distributed run.\n\nThis is a graph-partitioning problem: with orbitals as vertices and pairs as edges, the pairs form a line graph whose vertices we want split into equal parts with few cut edges – two pairs sharing an orbital share source support, so keeping them together shrinks the batch envelope. For this geometry (a planar lattice; every orbital lies in one plane) recursive coordinate bisection on the pair midpoints realizes that objective directly and deterministically, with no graph library: pairs sharing an orbital have nearby midpoints, so geometric compactness and a small edge cut coincide. Parts come out within one pair of equal size.\n\nLocality matters because the batch's interface is refined on the envelope of its sources: pairs that are far apart inflate the envelope and with it the interface, costing more in the solve than the batching saves.\n\nNote the pairs of one anchor are NOT forced into the same batch – BatchSpec.anchors is metadata (the manifest TSV and equality) and nothing computational depends on that grouping. Freeing it is what lets the parts hit k_target closely.\n\n\n\n\n\n","title":"BoundaryIntegral.build_batches"},{"category":"method","location":"api/#BoundaryIntegral.campaign_l_ec-Tuple{CampaignInput}","page":"API reference","text":"Edge/corner refinement target size: solve.l_ec if set, else min box Lz / 2^level · 1.01.\n\n\n\n\n\n","title":"BoundaryIntegral.campaign_l_ec"},{"category":"method","location":"api/#BoundaryIntegral.consolidate-Tuple{CampaignInput}","page":"API reference","text":"consolidate(c::CampaignInput)\n\nBetween solve and eval (single process): build\n\ntargets.jls: (; gidx, positions) — the sorted union of ALL stored batch supports;\nrho_store.jls: (; pair_ids, t_idx, tw) — per pair, its support's row indices in T and the contraction vector w .* ρ, in manifest batch order.\n\nErrors if any batch is missing. Idempotent.\n\n\n\n\n\n","title":"BoundaryIntegral.consolidate"},{"category":"method","location":"api/#BoundaryIntegral.consolidate_core-Tuple{Vector{BatchResult}, Any, CampaignInput}","page":"API reference","text":"consolidate_core(brs, dg, c) -> (targets, store)\n\nBuild the shared eval target set and the contraction store from a vector of BatchResults. targets = (; gidx, positions), store = (; pair_ids, t_idx, tw). No file IO.\n\n\n\n\n\n","title":"BoundaryIntegral.consolidate_core"},{"category":"method","location":"api/#BoundaryIntegral.density_centroid-Tuple{Any}","page":"API reference","text":"density_centroid(datagrid)\n\nCharge centroid sum(r*|phi|^2)/sum(|phi|^2) over the grid — the standard Wannier center. Assumes the function does not wrap the supercell boundary (interior-localized).\n\n\n\n\n\n","title":"BoundaryIntegral.density_centroid"},{"category":"method","location":"api/#BoundaryIntegral.dielectric_diagonal_scaling-Union{Tuple{DielectricInterface{P, Float64}}, Tuple{P}} where P<:BoundaryIntegral.AbstractPanel","page":"API reference","text":"dielectric_diagonal_scaling(interface) -> Vector{Float64}\n\nDiagonal right preconditioner P = G⁻¹ for the dielectric BIE operator A = G + Dᵀ + Cᵀ, where the contrast term is G = diag(t_P) with t_P = ½(ε_out+ε_in)/(ε_out−ε_in) per panel (this is −½γ_P in the paper's sign convention; the code carries the opposite sign in BOTH A and the RHS). Entry i is 1/t_{P(i)}, so A P = I + (Dᵀ+Cᵀ)P has its spectrum in one cluster at 1 rather than one cluster per distinct contrast.\n\nTwo properties worth knowing before using it:\n\n|γ| > 1 for any pair of positive permittivities, so |1/t| < 2: the scaling is uniformly bounded and cannot amplify a row.\nIt is a no-op whenever the geometry has a single contrast, since then P is a scalar multiple of I and GMRES is invariant under scalar scaling of (A, F). Expect identical iteration counts on single-box problems.\n\nIt clusters the spectrum of a multi-material geometry; it does NOT help the |γ| → 1 (conductor) limit, where the eigenvalues of I + (Dᵀ+Cᵀ)P migrate toward 0.\n\n\n\n\n\n","title":"BoundaryIntegral.dielectric_diagonal_scaling"},{"category":"method","location":"api/#BoundaryIntegral.enumerate_centers-Tuple{CampaignInput}","page":"API reference","text":"enumerate_centers(c::CampaignInput) -> Vector{CenterInfo}\n\nSnap each explicit [[orbital]] to its integer grid offset (against its template's grid) and record the realized Cartesian center. Orbital id = 1-based index in c.orbitals.\n\n\n\n\n\n","title":"BoundaryIntegral.enumerate_centers"},{"category":"method","location":"api/#BoundaryIntegral.enumerate_pairs-Tuple{Vector{CenterInfo}, Real}","page":"API reference","text":"Unique pairs (i ≤ j) with center distance ≤ cutoff. On-site pairs (i,i) included.\n\n\n\n\n\n","title":"BoundaryIntegral.enumerate_pairs"},{"category":"method","location":"api/#BoundaryIntegral.envelope_volume_source-Tuple{LatticeBatch}","page":"API reference","text":"envelope_volume_source(b::LatticeBatch)\n\nPer-point rss of the batch densities, as a VolumeSource (drives envelope refinement).\n\n\n\n\n\n","title":"BoundaryIntegral.envelope_volume_source"},{"category":"method","location":"api/#BoundaryIntegral.eval_batch-Tuple{CampaignInput, Int64}","page":"API reference","text":"eval_batch(c::CampaignInput, batch_id) -> path | nothing\n\nPost-eval phase for one batch: rebuild K sources from the BatchResult, evaluate Φ_a at the shared target set T, contract against every stored pair density, write K columns of V atomically. Skips if V file already complete.\n\n\n\n\n\n","title":"BoundaryIntegral.eval_batch"},{"category":"method","location":"api/#BoundaryIntegral.eval_batch_core-Tuple{BatchResult, Any, Any, Any, CampaignInput}","page":"API reference","text":"eval_batch_core(br, targets, store, dg, c) -> (source_pairs, V, rows, n_targets_used)\n\nEvaluate Φ_a = u_inc[ρ_a] + u[σ_a] at the shared target set, then contract against every stored pair density. Returns the batch's source pairs, the n_pairs × K block, the global row indices it covers (all of them) and the target count. No file IO.\n\nEvery batch evaluates every row. A symmetry-restricted variant existed briefly – V[a,b] = V[b,a], so a batch need only own rows >= min of its own columns, with assemble_v mirroring the rest – and it was removed after measurement, because it was a large net loss:\n\nit did cut the potential evaluation, 18.98 -> 11.19 G source-points (41%), about 2.4 node-hours on the 198-orbital campaign;\nbut a batch's rows then cover only a SUBSET of the shared target set, so the contraction needed a Dict remapping every global target index into the subset – built per batch (up to 7.3M entries) and probed once per target per row.\n\nMeasured on latticeconvl3eps2.4kb31 batch 1 (K = 31, full target set): the potential evaluation takes 235 s standalone, while the campaign's t_phi for that same batch was 1896 s. The 1661 s difference is this remapping and the serial loop around it – 87% of the batch, to save 2.4 node-hours across the campaign. The docstring of the removed function asserted the opposite (\"the saving is NOT in the contractions – those are cheap dot products\"); it was never measured.\n\nEvaluating every row also makes the eval cost n_pairs × n_targets, hence independent of the batching, and it removes a second-order accuracy concern: with subsets, each batch refined its interface (_refine_interface_for_targets) against a DIFFERENT target set, so the quadrature a given entry received depended on which batch happened to own it.\n\nassemble_v then mirrors nothing, and its max_rel_asym recovers its full meaning as an end-to-end check: every off-diagonal entry is computed twice, independently.\n\n\n\n\n\n","title":"BoundaryIntegral.eval_batch_core"},{"category":"method","location":"api/#BoundaryIntegral.evaluate_batch_potential-Tuple{Any, AbstractMatrix, Vector{<:VolumeSource{Float64, 3}}, Matrix{Float64}}","page":"API reference","text":"evaluate_batch_potential(interface, Σ, sources, targets;\n    lhs_tol, volume_tol, c_pad=5.0, range_factor=5.0) -> Φ (n_targets × K)\n\nTotal potential Φ_a = u_inc[ρ_a] + u[σ_a] of a solved batch at arbitrary targets.\n\nu[σ_a]: corrected layer-potential map (FMM + hcubature near correction) built ONCE for the target set, applied per column of Σ.\nu_inc[rho_a]: batch-level near/far split on the Section 3 padded near region Bpad (Eq. 3.9), `hn = cpad * ||Arho||_2. Near targets: TKM via PrecomputedVolumeField per source, on the paper's Fourier geometry (Eqs. 3.11, 3.16). Far targets: ONEnd = K` point-charge FMM (potential/4pi) over the screened quadrature points.\n\nc_pad is DIMENSIONLESS, a multiple of the lattice spacing; default 5.0.\n\nAll sources must share identical grid points (the batch convention); this is checked.\n\nConventions match four_index_matrix: TKM returns the 1/(4π|r|)-normalised potential and is used without further scaling; the scattered layer potential is the output of laplace3d_pottrg_fmm3d_corrected_hcubature applied to each column of Σ.\n\n\n\n\n\n","title":"BoundaryIntegral.evaluate_batch_potential"},{"category":"method","location":"api/#BoundaryIntegral.four_index_integrals-Tuple{AbstractString}","page":"API reference","text":"four_index_integrals(toml_path::AbstractString) -> (; pair_ids, V)\n\nLoad a campaign from toml_path and run the full four-index pipeline in memory.\n\n\n\n\n\n","title":"BoundaryIntegral.four_index_integrals"},{"category":"method","location":"api/#BoundaryIntegral.four_index_integrals-Tuple{CampaignInput}","page":"API reference","text":"four_index_integrals(c::CampaignInput) -> (; pair_ids, V)\n\nRun the full four-index pipeline in memory (no files written).  Reuses the same *_core functions as the file-based phases to guarantee identical numerics.\n\n\n\n\n\n","title":"BoundaryIntegral.four_index_integrals"},{"category":"method","location":"api/#BoundaryIntegral.four_index_matrix-Tuple{Any, Vector{<:VolumeSource{Float64, 3}}, AbstractMatrix}","page":"API reference","text":"four_index_matrix(interface, sources, Σ; lhs_tol, volume_tol, c_pad=5.0,\n    range_factor=5.0) -> K×K\n\nFour-index contraction V[a,b] = ∫ ρ_a (u_inc[ρ_b] + u[σ_b]) for K sources sharing one interface. Σ is the N × K density matrix from solve_dielectric_box3d_block. The incident term u_inc is evaluated with a PrecomputedVolumeField of each screened source (tolerance volume_tol, padding c_pad); the induced term u[σ] with the near-corrected FMM target evaluation at tolerance lhs_tol. Targets are the quadrature points of sources[1], so all sources must share one grid.\n\n\n\n\n\n","title":"BoundaryIntegral.four_index_matrix"},{"category":"method","location":"api/#BoundaryIntegral.in_near_region-Tuple{NearFieldGeometry, AbstractMatrix, Integer}","page":"API reference","text":"in_near_region(g, targets, i) -> Bool\n\nEq. (3.9): is column i of the 3 × n matrix targets inside B_pad?\n\n\n\n\n\n","title":"BoundaryIntegral.in_near_region"},{"category":"method","location":"api/#BoundaryIntegral.is_complete_batch-Tuple{AbstractString}","page":"API reference","text":"True iff path exists and loads as a valid, current-version BatchResult.\n\n\n\n\n\n","title":"BoundaryIntegral.is_complete_batch"},{"category":"method","location":"api/#BoundaryIntegral.laplace3d_pot_panel_hcubature-Union{Tuple{T}, Tuple{FlatPanel{T, 3}, Matrix{T}, Vector{Int64}, T}} where T","page":"API reference","text":"laplace3d_pot_panel_hcubature(panel_src, targets, target_ids, atol; threaded = true)\n\nAdaptive-quadrature potential of one source panel at target_ids. threaded = false runs the target loop serially, for callers that are ALREADY inside a parallel region – laplace3d_pottrg_corrections_hcubature parallelises over panels instead, so threading here too would nest parallel regions.\n\n\n\n\n\n","title":"BoundaryIntegral.laplace3d_pot_panel_hcubature"},{"category":"method","location":"api/#BoundaryIntegral.lattice_grid_steps-Tuple{Any, AbstractMatrix, Tuple{Int64, Int64, Int64}}","page":"API reference","text":"lattice_grid_steps(datagrid, primvec, n::NTuple{3,Int}) -> NTuple{3,Int}\n\nInteger grid-step offset of the lattice translation n1·a1 + n2·a2 + n3·a3 (the global-frame offset of a translated orbital instance). Errors if a lattice vector is not grid-commensurate. The offset is interpreted as a frame translation, not a circshift.\n\n\n\n\n\n","title":"BoundaryIntegral.lattice_grid_steps"},{"category":"method","location":"api/#BoundaryIntegral.lattice_shifted_datagrid-Tuple{Any, AbstractMatrix, Tuple{Int64, Int64, Int64}}","page":"API reference","text":"lattice_shifted_datagrid(datagrid, primvec, (n1,n2,n3))\n\nReturn a copy of datagrid with its density translated by the lattice vector n1·a1 + n2·a2 + n3·a3 via an exact integer circshift (same grid; periodic).\n\n\n\n\n\n","title":"BoundaryIntegral.lattice_shifted_datagrid"},{"category":"method","location":"api/#BoundaryIntegral.lattice_spacing-Union{Tuple{VolumeSource{T, 3}}, Tuple{T}} where T","page":"API reference","text":"lattice_spacing(vs) -> T\n\nThe characteristic spacing h = ||A_rho||_2 of Eq. (3.3): the largest singular value of the primitive sampling-cell basis. Falls back to _estimate_source_spacing (minimum nearest-neighbour distance) when the source carries no lattice basis; the two agree for a cubic grid.\n\nThis is the spacing for h_n = c_pad * h only. k_max keeps _estimate_source_spacing, because Section 3.3 leaves k_Nyq open for a general sampling lattice.\n\n\n\n\n\n","title":"BoundaryIntegral.lattice_spacing"},{"category":"method","location":"api/#BoundaryIntegral.load_batch_result-Tuple{AbstractString}","page":"API reference","text":"load_batch_result(path) -> BatchResult\n\nDeserialize and validate (format version, internal shape consistency). Throws on any problem — callers treating a batch as done must use is_complete_batch first.\n\n\n\n\n\n","title":"BoundaryIntegral.load_batch_result"},{"category":"method","location":"api/#BoundaryIntegral.near_field_geometry-Union{Tuple{VolumeSource{T, 3}}, Tuple{T}} where T","page":"API reference","text":"near_field_geometry(vs; c_pad = 5.0) -> NearFieldGeometry\n\nAssemble the Section 3 geometry for vs. dks uses prevfloat so the real-space period is strictly greater than the Eq. (3.16) bound, keeping the trapezoidal sum inside the aliasing-free set rather than exactly on its boundary.\n\n\n\n\n\n","title":"BoundaryIntegral.near_field_geometry"},{"category":"method","location":"api/#BoundaryIntegral.pending_batches-Tuple{CampaignInput, Symbol}","page":"API reference","text":"pending_batches(c::CampaignInput, phase::Symbol) -> Vector{Int}\n\nBatch ids still to do, derived from files on disk. :solve → no complete batch file; :eval → no complete V file.\n\n\n\n\n\n","title":"BoundaryIntegral.pending_batches"},{"category":"method","location":"api/#BoundaryIntegral.plot_campaign_geometry-Tuple","page":"API reference","text":"plot_campaign_geometry(c::CampaignInput; kwargs...) -> Figure\nplot_campaign_geometry(toml_path::AbstractString; kwargs...) -> Figure\n\n3D view of a campaign's geometry: each dielectric box as a wireframe cuboid colored by its permittivity ε, plus the orbital positions as a scatter colored by sublattice type. Requires a Makie backend (using CairoMakie/GLMakie). Implemented in the Makie extension.\n\n\n\n\n\n","title":"BoundaryIntegral.plot_campaign_geometry"},{"category":"method","location":"api/#BoundaryIntegral.prepare-Tuple{CampaignInput}","page":"API reference","text":"prepare(c::CampaignInput)\n\nPhase 1 (single process): enumerate centers/pairs/batches and write centers.tsv + manifest.tsv under c.root. Idempotent: existing files are kept (delete them to re-prepare). Guards against parameter drift via manifest.params.\n\n\n\n\n\n","title":"BoundaryIntegral.prepare"},{"category":"method","location":"api/#BoundaryIntegral.rhs_dielectric_box3d_field-Union{Tuple{P}, Tuple{DielectricInterface{P, Float64}, PrecomputedVolumeField{Float64}, Float64}} where P<:BoundaryIntegral.AbstractPanel","page":"API reference","text":"rhs_dielectric_box3d_field(interface, field, eps_src) -> Vector\n\nDielectric-interface RHS assembled from a PrecomputedVolumeField (in-box targets: type-2 NUFFT on stored coefficients; out-of-box: FMM at the field tolerance). Drop-in alternative to rhs_dielectric_box3d_hybrid when the same source density is used for many assemblies/evaluations. Returns the same sign convention as rhs_dielectric_box3d / rhs_dielectric_box3d_fmm3d (an internal negation is applied to match).\n\n\n\n\n\n","title":"BoundaryIntegral.rhs_dielectric_box3d_field"},{"category":"method","location":"api/#BoundaryIntegral.rhs_dielectric_box3d_fmm3d-Union{Tuple{P}, Tuple{DielectricInterface{P, Float64}, Vector{<:VolumeSource{Float64, 3}}, Float64}} where P<:FlatPanel{Float64, 3}","page":"API reference","text":"rhs_dielectric_box3d_fmm3d(interface, vss::Vector{VolumeSource}, thresh) -> N×K matrix\n\nMulti-source right-hand side: column k is the BIE RHS for source vss[k], mirroring the single-source rhs_dielectric_box3d_fmm3d(interface, vs, thresh) (each source screened via the interface, eps_src = 1). When all (screened) sources share identical grid points, a single nd = K batched FMM is used; otherwise the per-source method is looped.\n\n\n\n\n\n","title":"BoundaryIntegral.rhs_dielectric_box3d_fmm3d"},{"category":"function","location":"api/#BoundaryIntegral.run_phase","page":"API reference","text":"run_phase(c, phase; workers=0)\n\nParallel driver for :solve/:eval over pending_batches. Implemented by the BoundaryIntegralDistributedExt extension (load Distributed + SlurmClusterManager). Without the extension loaded, this errors with a hint.\n\n\n\n\n\n","title":"BoundaryIntegral.run_phase"},{"category":"method","location":"api/#BoundaryIntegral.save_batch_result-Tuple{AbstractString, BatchResult}","page":"API reference","text":"Atomic write: serialize to <path>.tmp.<pid>, then rename onto path.\n\n\n\n\n\n","title":"BoundaryIntegral.save_batch_result"},{"category":"method","location":"api/#BoundaryIntegral.screened_volume_source-Union{Tuple{T}, Tuple{T, T, T, VolumeSource{T, 3}, T, T, BoundaryIntegral.AbstractScreeningMode}} where T","page":"API reference","text":"screened_volume_source(Lx, Ly, Lz, vs, eps_in, eps_out, mode; tol = ...)\nscreened_volume_source(interface, vs, mode; tol = ...)\n\nApply the screened density selected by mode to each volume sample.\n\n\n\n\n\n","title":"BoundaryIntegral.screened_volume_source"},{"category":"method","location":"api/#BoundaryIntegral.snap_orbital-Tuple{Any, Tuple{Float64, Float64, Float64}, Tuple{Float64, Float64, Float64}}","page":"API reference","text":"snap_orbital(datagrid, centroid, pos) -> NTuple{3,Int}\n\nInteger grid-step offset that places an orbital (template datagrid, density centroid centroid) closest to Cartesian pos. Solves pos - centroid = G * s for real s in the grid-step basis G = [At/nx Bt/ny Ct/nz] (handles non-orthogonal a1/a2), then rounds. Warns if the snap moves the center by more than half the largest grid step; errors if the z-offset is not (near-)integer (a planar campaign — a fractional z is almost certainly a frame/unit mistake).\n\n\n\n\n\n","title":"BoundaryIntegral.snap_orbital"},{"category":"method","location":"api/#BoundaryIntegral.solve_batch-Tuple{CampaignInput, Int64}","page":"API reference","text":"solve_batch(c::CampaignInput, batch_id) -> path | nothing\n\nSolve phase for one batch: assemble pair densities, envelope-refine ONE shared interface, block-GMRES, write the BatchResult atomically. Skips if output already exists.\n\n\n\n\n\n","title":"BoundaryIntegral.solve_batch"},{"category":"method","location":"api/#BoundaryIntegral.solve_batch_core-Tuple{CampaignInput, BatchSpec, AbstractVector, AbstractDict{Int64, OrbitalInstance}}","page":"API reference","text":"solve_batch_core(c, spec, grids, insts) -> BatchResult\n\nAssemble pair densities, build the shared envelope interface, block-GMRES, and pack the result. No file IO — the caller decides whether to persist.\n\n\n\n\n\n","title":"BoundaryIntegral.solve_batch_core"},{"category":"method","location":"api/#BoundaryIntegral.solve_dielectric_box3d_block-Union{Tuple{P}, Tuple{DielectricInterface{P, Float64}, Vector{<:VolumeSource{Float64, 3}}}} where P<:FlatPanel{Float64, 3}","page":"API reference","text":"solve_dielectric_box3d_block(interface, vss::Vector{VolumeSource}; kw...) -> (Σ, stats)\n\nLow-level multi-RHS solve on a prebuilt shared interface: builds the batched operator and the N×K RHS, then block-GMRES solves A Σ = F for the layer densities Σ (N×K).\n\ncorrect_edges forwards to [batched_lhs_dielectric_box3d_fmm3d_corrected] and defaults to true. Setting it false drops rim (is_edge) panels from the near list as both source and target and builds NO touching-pair (adaptive) corrections at all, which degrades the conditioning of A badly as the mesh is refined at the edges: on the three-material benchmark of examples/diag_precond_experiment.jl at p=6, l_ec=1.25 (N≈2.4e4), GMRES needs 179 iterations without the edge correction versus 27 with it (828 vs 51 at ε₂=200), so the correction pays for its setup many times over.\n\nprecondition (default true) applies the diagonal contrast scaling of dielectric_diagonal_scaling as a RIGHT preconditioner. Right rather than left so that block GMRES keeps minimizing the true residual ‖F − AΣ‖ and rtol retains its meaning; Krylov.block_gmres applies N to the returned solution, so Σ needs no post-scaling. Same solution, fewer iterations: 1.6–2.1× on the multi-material benchmark of examples/diag_precond_experiment.jl, and exactly a no-op for a single-contrast geometry.\n\n\n\n\n\n","title":"BoundaryIntegral.solve_dielectric_box3d_block"},{"category":"method","location":"api/#BoundaryIntegral.solve_dielectric_lattice_batch-Tuple{Vector{@NamedTuple{center::Tuple{Float64, Float64, Float64}, Lx::Float64, Ly::Float64, Lz::Float64}}, Vector{Float64}, Float64, LatticeBatch}","page":"API reference","text":"solve_dielectric_lattice_batch(boxes, epses, eps_out, b::LatticeBatch; kw...)\n    -> (; sigma, interface, sources, stats)\n\nSolve an explicit-pair batch: ONE shared interface refined on the batch envelope, then block GMRES over all pair densities.\n\n\n\n\n\n","title":"BoundaryIntegral.solve_dielectric_lattice_batch"},{"category":"method","location":"api/#BoundaryIntegral.source_box-Union{Tuple{VolumeSource{T, 3}}, Tuple{T}} where T","page":"API reference","text":"source_box(vs) -> (lo, hi, l)\n\nThe source box B of Eq. (3.7): the bounding box of the samples inflated by half a lattice cell, so that the quadrature cells tile B and B contains the full support of the piecewise-constant density. The per-axis half-extent is (1/2) * sum_j |A_rho[alpha, j]|, or h/2 when no basis is available.\n\n\n\n\n\n","title":"BoundaryIntegral.source_box"},{"category":"method","location":"api/#BoundaryIntegral.theta_ODE_det-Tuple{AbstractVector, AbstractVector, Any}","page":"API reference","text":"theta_ODE_det(a,e,g)\n\nreturn determinant for theta ODE transmission for multi-junction dielectric\nrelative premittivity vector `e` and angles `a` (length one less than `e`,\nsince the last angle is known), and trial power `g`\n(so u''+g^2u = 0 is the theta ODE in each material).\nZero is returned iff `g` is a (nonlin) eigenvalue of the periodic ODE on\n[0,2pi].\nThe last `e` value (only) may be `Inf`, in which case Dirichlet BCs are used\non its angle endpoints (and the incoming u' to final u value map used).\nNote: Parity issues avoided due to solving the 2x2 transmission matrix\non the entire [0,2pi] domain. (All parities are combined.)\n\n\n\n\n\n","title":"BoundaryIntegral.theta_ODE_det"},{"category":"method","location":"api/#BoundaryIntegral.volume_field_gradient-Union{Tuple{T}, Tuple{PrecomputedVolumeField{T}, AbstractMatrix{<:Real}}} where T","page":"API reference","text":"volume_field_gradient(field, targets) -> 3 x n Matrix\n\nGradient of the precomputed field at targets (3 x n), free-space 1/(4π r) normalization. In-box targets via type-2 NUFFT, out-of-box via FMM at the field tolerance.\n\n\n\n\n\n","title":"BoundaryIntegral.volume_field_gradient"},{"category":"method","location":"api/#BoundaryIntegral.volume_field_potential-Union{Tuple{T}, Tuple{PrecomputedVolumeField{T}, AbstractMatrix{<:Real}}} where T","page":"API reference","text":"volume_field_potential(field, targets) -> Vector\n\nPotential of the precomputed field at targets (3 x n), free-space 1/(4π r) normalization. In-box targets via type-2 NUFFT, out-of-box via FMM at the field tolerance.\n\n\n\n\n\n","title":"BoundaryIntegral.volume_field_potential"},{"category":"method","location":"api/#BoundaryIntegral.with_density-Union{Tuple{T}, Tuple{VolumeSource{T, 3}, Vector{T}}} where T","page":"API reference","text":"with_density(vs, density) -> VolumeSource\n\nCopy of vs with density replaced, preserving positions, weights, and lattice_basis. Use this instead of rebuilding through the flat constructor, which would silently drop the lattice basis.\n\n\n\n\n\n","title":"BoundaryIntegral.with_density"},{"category":"method","location":"api/#BoundaryIntegral.write_v_table-Tuple{AbstractString, Vector{Tuple{Int64, Int64}}, AbstractMatrix}","page":"API reference","text":"write_v_table(path, pair_ids, V)\n\nWrite the four-index result as a text table: header i\\tj\\tk\\tl\\tV, then one row per entry i j k l value where (i,j) = pair_ids[row], (k,l) = pair_ids[col], value = V[row, col]. All entries (dense). Atomic (tmp + rename).\n\n\n\n\n\n","title":"BoundaryIntegral.write_v_table"},{"category":"method","location":"api/#BoundaryIntegral.TKM3D.estimate_kcut3dc-Tuple{AbstractMatrix{<:Real}}","page":"API reference","text":"estimate_kcut3dc(sources; charges, tol, eps=1e-12)\n\nEstimate the smallest radial spectral cutoff kcut such that the relative pointwise tail of the type-1 NUFFT spectrum satisfies\n\nmax_{|q| > kcut} |F(q)| / max_q |F(q)| < tol\n\nusing a Nyquist-limited anisotropic mode box inferred from the average positive source-coordinate gaps on each axis.\n\n\n\n\n\n","title":"BoundaryIntegral.TKM3D.estimate_kcut3dc"},{"category":"method","location":"api/#BoundaryIntegral.TKM3D.ltkm3dc-Tuple{Real, AbstractMatrix{<:Real}}","page":"API reference","text":"ltkm3dc(eps, sources; charges, targets=nothing, pg=0, pgt=0, kmax=nothing,\n        nthreads=_ltkm_default_nthreads())\n\nContinuous free-space Laplace TKM evaluator with arbitrary source points and preweighted quadrature masses. nthreads caps the internal FINUFFT transforms (default 16, or env TKM3D_FINUFFT_NTHREADS) to avoid the FFTW high-thread-count plan pathology; the FMM path is unaffected.\n\n\n\n\n\n","title":"BoundaryIntegral.TKM3D.ltkm3dc"},{"category":"section","location":"campaign/#Lattice-campaigns","page":"Lattice campaigns","text":"For large four-index runs over many orbital centers on a lattice, the package provides a file-backed pipeline driven from a TOML campaign description. It groups pair densities into batches that share one boundary operator, solves each batch, then evaluates and contracts every pair against a shared target set.","title":"Lattice campaigns"},{"category":"section","location":"campaign/#Pipeline","page":"Lattice campaigns","text":"prepare  →  solve_batch  →  consolidate  →  eval_batch  →  assemble_v\n\nprepare — enumerate centers, neighbor pairs, and batches; write the manifest.\nsolve_batch — per batch: assemble the shared interface, block-GMRES, store σ + screened ρ.\nconsolidate — build the shared evaluation target set and the contraction store.\neval_batch — per batch: evaluate Φ at the shared targets and contract all pairs into V columns.\nassemble_v — gather all V columns into the dense matrix; write V_full.tsv + report.txt.\n\nEvery phase writes its outputs atomically (temp file + rename), so a run is crash-safe and restartable: re-running a phase skips already-completed batches, and a killed job is recovered simply by rerunning the phase. pending_batches(c, :solve | :eval) reports what is left to do.","title":"Pipeline"},{"category":"section","location":"campaign/#Campaign-TOML","page":"Lattice campaigns","text":"name = \"demo\"                          # campaign name\nroot = \"/path/to/output\"               # output dir: manifest.tsv, batches/, V/, logs/\ntemplates = [\"orb1.xsf\", \"orb2.xsf\"]   # type index -> .xsf path (relative to this file)\n\n[[orbital]]                            # one entry per orbital; id = 1-based order\ntype = 1                               # index into `templates`\nx = 0.0                                # Cartesian center in the templates' frame\ny = 0.0\nz = 7.5\n\n[pairing]\nneighbor_cutoff = 2.6                  # pair orbitals within this distance (default: Inf)\n# pairs = [[1, 2], [1, 3]]             # OR give explicit pair overrides instead\n\n[dielectrics]\neps_out = 1.0\nboxes = [[0.0, 0.0, 7.5, 90.0, 90.0, 3.35, 3.5]]   # rows of [cx cy cz Lx Ly Lz eps]\n\n[solve]\nn_quad = 6\nedge_refine_level = 2                  # or set `l_ec` directly\nrhs_tol = 1e-3\nlhs_tol = 1e-5\ngmres_rtol = 1e-5\nsupport_rtol = 1e-4\nvolume_tol = 1e-5\nmax_order = 8\nmax_depth = 128\n\n[batching]\nn_centers_per_batch = 1\n\n[eval]\nfar_pad_steps = 2.0","title":"Campaign TOML"},{"category":"section","location":"campaign/#Running-serially","page":"Lattice campaigns","text":"using BoundaryIntegral\n\nc = load_campaign(\"campaign.toml\")\nprepare(c)\nfor id in pending_batches(c, :solve); solve_batch(c, id); end\nconsolidate(c)\nfor id in pending_batches(c, :eval); eval_batch(c, id); end\nassemble_v(c)\n\nThe whole pipeline can also be run in memory, without writing files, via four_index_integrals(\"campaign.toml\") (returns (; pair_ids, V)).","title":"Running serially"},{"category":"section","location":"campaign/#Running-distributed","page":"Lattice campaigns","text":"The :solve and :eval phases parallelize over batches. run_phase is provided by a package extension; load its weak dependencies (Distributed and SlurmClusterManager) to enable it. Spawning policy: inside a Slurm allocation with more than one task it uses SlurmManager() (one worker per task); otherwise it spawns workers local processes; with workers = 0 and no allocation it runs inline.\n\nusing BoundaryIntegral, Distributed, SlurmClusterManager\n\nc = load_campaign(\"campaign.toml\")\nprepare(c)\nrun_phase(c, :solve; workers = 4)   # local workers (or SlurmManager inside an allocation)\nconsolidate(c)\nrun_phase(c, :eval; workers = 4)\nassemble_v(c)\n\nA campaign is typically driven by a small script over this API (the package itself ships no CLI). On Slurm, run one task per node and give each task the whole node via threads:\n\n#!/bin/bash\n#SBATCH --nodes=4\n#SBATCH --ntasks-per-node=1\n#SBATCH --cpus-per-task=96\n#SBATCH --output=logs/solve_%j.out\nset -euo pipefail\n\n# Pin both thread pools — unpinned threads silently corrupt timings.\nexport OMP_NUM_THREADS=$SLURM_CPUS_PER_TASK\nexport OPENBLAS_NUM_THREADS=$SLURM_CPUS_PER_TASK\nexport JULIA_GLUE_THREADS=8\n\njulia --project -t \"$JULIA_GLUE_THREADS\" driver.jl campaign.toml solve\n\nwhere driver.jl parses <campaign.toml> <phase> and calls the phase functions above. Notes for cluster runs:\n\nPrecompile on the head process first (using Pkg; Pkg.precompile() before loading Distributed/SlurmClusterManager) so workers don't race to precompile over a shared filesystem.\nOne worker per node (--ntasks-per-node=1, --cpus-per-task=<cores>); the FMM saturates many cores, so node-sized tasks are the right grain.\nCrash/walltime recovery: just resubmit the same phase — completed batches are skipped.","title":"Running distributed"},{"category":"section","location":"#BoundaryIntegral.jl","page":"Home","text":"BoundaryIntegral.jl solves Laplace problems with dielectric interfaces in 2D and 3D using boundary integral equations. It provides single- and double-layer operators, panel-based quadrature with adaptive refinement, FMM acceleration with near-field correction, volume charge sources, many-right-hand-side (block) solves, and a batched four-index integral pipeline that can run distributed across a cluster.\n\nFor large four-index runs over many lattice sites, see Lattice campaigns. The full list of functions and types is in the API reference.","title":"BoundaryIntegral.jl"},{"category":"section","location":"#Features","page":"Home","text":"Laplace single- and double-layer operators in 2D and 3D.\nDielectric interface builders for box geometries, with adaptive panel refinement.\nDirect and FMM-accelerated linear operators (with near-field correction) for iterative solves.\nVolume charge sources, plus a reusable precomputed spectral field (PrecomputedVolumeField) for fast repeated potential/gradient evaluation and right-hand-side assembly.\nMany-right-hand-side (block) solves and four-index Coulomb integrals.\nA batched, restartable lattice campaign pipeline for large four-index runs, with optional distributed/Slurm execution.\nLinear algebra helpers (solve_lu, solve_gmres).\nOptional Makie visualization extension.\n\nThe public API is exported, so using BoundaryIntegral brings it into scope directly; see the API reference.","title":"Features"},{"category":"section","location":"#Installation","page":"Home","text":"BoundaryIntegral.jl requires Julia 1.10 or later.\n\nusing Pkg\nPkg.add(\"BoundaryIntegral\")\n\nFor local development, clone the repository and run:\n\njulia --project -e 'using Pkg; Pkg.instantiate()'","title":"Installation"},{"category":"section","location":"#Quickstart-(2D)","page":"Home","text":"using BoundaryIntegral\n\n# single_dielectric_box2d(Lx, Ly, n_quad, l_panel, l_corner, eps_in, eps_out[, T])\ninterface = single_dielectric_box2d(1.0, 1.0, 8, 0.2, 0.05, 5.0, 1.0, Float64)\nlhs = lhs_dielectric_box2d(interface)\nrhs = rhs_dielectric_box2d(interface, PointSource((0.1, 0.1), 1.0), 5.0)\n\nsigma = solve_lu(lhs, rhs)","title":"Quickstart (2D)"},{"category":"section","location":"#Quickstart-(3D)","page":"Home","text":"using BoundaryIntegral\n\n# single_dielectric_box3d(Lx, Ly, Lz, n_quad, l_ec, eps_in, eps_out[, T])\ninterface = single_dielectric_box3d(1.2, 0.8, 0.6, 4, 0.2, 4.0, 1.0, Float64)\nlhs = lhs_dielectric_box3d(interface)\nrhs = rhs_dielectric_box3d(interface, PointSource((0.1, 0.1, 0.1), 1.0), 4.0)\n\nsigma = solve_lu(lhs, rhs)","title":"Quickstart (3D)"},{"category":"section","location":"#FMM-accelerated-solves","page":"Home","text":"For larger problems, replace the dense operator with an FMM-accelerated LinearMap and solve with GMRES. In 3D, lhs_dielectric_box3d_fmm3d_corrected adds the near-field correction needed for accuracy near the surface.\n\nusing BoundaryIntegral\n\ninterface = single_dielectric_box2d(1.0, 1.0, 8, 0.2, 0.05, 5.0, 1.0, Float64)\nlhs_fmm = lhs_dielectric_box2d_fmm2d(interface, 1e-12)\nrhs = rhs_dielectric_box2d(interface, PointSource((0.1, 0.1), 1.0), 5.0)\n\n# solve_gmres(A, b, atol, rtol)\nsigma = solve_gmres(lhs_fmm, rhs, 1e-12, 1e-12)","title":"FMM-accelerated solves"},{"category":"section","location":"#Volume-sources-and-precomputed-fields","page":"Home","text":"A VolumeSource is a volume charge density sampled on a quadrature grid: positions (3 × N), quadrature weights, and density values. Its free-space Laplace potential can drive the right-hand side of a dielectric problem on an interface that is adaptively refined to resolve that source:\n\nusing BoundaryIntegral\n\n# A Gaussian charge sampled on a uniform midpoint grid: positions (3×N), weights, density.\nn, h = 24, 1.2 / 24\nxs = range(-0.6 + h / 2, 0.6 - h / 2; length = n)\npositions = reduce(hcat, [[x, y, z] for x in xs for y in xs for z in xs])\ndensity = [exp(-sum(abs2, p) / (2 * 0.08^2)) for p in eachcol(positions)]\nvs = VolumeSource(positions, fill(h^3, n^3), density)\n\n# single_dielectric_box3d_rhs_adaptive(Lx, Ly, Lz, n_quad, source,\n#                                       eps_src, l_ec, rhs_atol, eps_in, eps_out[, T])\ninterface = single_dielectric_box3d_rhs_adaptive(\n    1.0, 1.0, 1.0, 4, vs, 1.0, 0.25, 1e-3, 4.0, 1.0, Float64)\n\nrhs = rhs_dielectric_box3d_hybrid(interface, vs, 1.0, 1e-6)\n\nWhen the same source is reused for many target batches or assemblies, build the field once and reuse it. PrecomputedVolumeField stores a truncated spectral representation; in-box targets are evaluated with a type-2 NUFFT and out-of-box targets with the FMM:\n\nfield = PrecomputedVolumeField(vs; tol = 1e-6)\n\nphi  = volume_field_potential(field, targets)   # targets: 3×n  ->  length-n potential\ngrad = volume_field_gradient(field, targets)    #              ->  3×n gradient\nrhs  = rhs_dielectric_box3d_field(interface, field, 1.0)","title":"Volume sources and precomputed fields"},{"category":"section","location":"#Many-right-hand-sides-and-four-index-integrals","page":"Home","text":"For several sources sharing one interface, assemble and block-solve all right-hand sides at once, then contract into the four-index Coulomb matrix V[a, b]:\n\nusing BoundaryIntegral\n\n# vss::Vector{VolumeSource} on a shared interface that resolves all of them\n# (e.g. built with multi_dielectric_box3d_rhs_adaptive)\nsigma, stats = solve_dielectric_box3d_block(interface, vss; rtol = 1e-6)\nV = four_index_matrix(interface, vss, sigma; lhs_tol = 1e-6, volume_tol = 1e-6)","title":"Many right-hand sides and four-index integrals"},{"category":"section","location":"#Visualization","page":"Home","text":"Plotting helpers (viz_2d, viz_3d, viz_3d_surface, viz_3d_interface_solution, viz_3d_zslice, plot_campaign_geometry) live in a Makie package extension. Install Makie and a backend, then load the backend alongside BoundaryIntegral:\n\nusing Pkg\nPkg.add([\"Makie\", \"CairoMakie\"])\n\nusing CairoMakie\nusing BoundaryIntegral\n\ninterface = single_dielectric_box3d(1.2, 0.8, 0.6, 4, 0.2, 4.0, 1.0, Float64)\nfig = viz_3d(interface)","title":"Visualization"}]
}
